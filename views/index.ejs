<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>감성길드 갤러리</title>
  <style>

    /* 이벤트 제목을 담을 연두색 띠 스타일 */
    .event-banner {
      background-color: #9ACD32; /* 연두색 */
      text-align: center;
      padding: 20px 0;
      color: white;
      font-size: 1.8rem;
    }

    .event-banner h1 {
      margin: 0;
      font-size: 3rem;
    }
    h3 {
      text-align: center;
      margin-top: 5px;
      font-size: 1rem;
    }
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .gallery {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .photo-item {
      text-align: center;
      flex: 1 0 45%;
      box-sizing: border-box;
      cursor: pointer; /* 클릭 가능하도록 */
    }

    .photo-item img {
      width: 100%;
      max-width: 300px;
      max-height: 300px;
      object-fit: cover;
      border-radius: 10px;
    }

    .photo-item h3 {
      margin-top: 10px;
      font-size: 18px;
    }

    /* 모바일 화면에서는 사진 하나씩 스크롤 */
    @media (max-width: 768px) {
      .gallery {
        flex-direction: column;
        align-items: center;
      }

      .photo-item {
        width: 90%;
        margin-bottom: 20px;
      }

      .photo-item img {
        max-width: 100%;
        max-height: 400px;
      }
    }

    /* 모달 스타일 */
    .modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto;
      background-color: rgba(0, 0, 0, 0.8);
      padding-top: 60px;
    }

    .modal-content {
      margin: auto;
      display: block;
      width: 80%;
      max-width: 700px;
    }

    .close {
      position: absolute;
      top: 15px;
      right: 35px;
      color: #f1f1f1;
      font-size: 40px;
      font-weight: bold;
      transition: 0.3s;
    }

    .close:hover,
    .close:focus {
      color: #bbb;
      text-decoration: none;
      cursor: pointer;
    }
  </style>
</head>
<body>
    <div class="event-banner">
  <h1>감성 길드 코디 이벤트</h1>
  </div>
  <h3>참여 기간 ~4/25</h3>
  <h3>투표 기간 4/26 ~ 4/27</h3>
  <h3>투표는 카톡으로 진행합니다!</h3>
  <div class="gallery">
    <% images.forEach(image => { %>
      <div class="photo-item" onclick="openModal('<%= image.src %>')">
        <img src="<%= image.src %>" alt="<%= image.title %>">
        <h3><%= image.title %></h3>
      </div>
    <% }) %>
  </div>

  <!-- 모달 -->
  <div id="myModal" class="modal">
    <span class="close" onclick="closeModal()">&times;</span>
    <img class="modal-content" id="modalImage">
  </div>

  <script>
    // 모달 열기
    function openModal(src) {
      document.getElementById("myModal").style.display = "block";
      document.getElementById("modalImage").src = src;
    }

    // 모달 닫기
    function closeModal() {
      document.getElementById("myModal").style.display = "none";
    }

    // 모달 외부 클릭 시 닫기
    window.onclick = function(event) {
      var modal = document.getElementById("myModal");
      if (event.target == modal) {
        closeModal();
      }
    }
  </script>
</body>
</html>
